# 🧠 Архитектура системы контроллеров

Документ описывает внутреннее устройство архитектуры контроллеров, абстракций и общей логики в системе `gravity_controller_operator`.

---

## 🎯 Цели архитектуры

- Предоставить **унифицированный интерфейс** для работы с различными физическими контроллерами
- Разделить **логическое состояние точек** и **низкоуровневое управление устройством**
- Упростить **добавление новых устройств** без изменения основной логики

---

## 🧩 Компоненты

### 1. `SoftStateMixin`
Хранит словарь состояний точек (DI или Relay):
```python
{
  0: {"state": 1, "changed": datetime, "addr": 0},
  1: {...},
  ...
}
```
- Метод `update_state` обновляет состояние и метку времени
- Метод `get_point()` возвращает конкретный канал

Используется как базовый класс для всех интерфейсов.

---

### 2. `DIInterface`, `RelayInterface`
Наследуют `SoftStateMixin` и соответствующий физический интерфейс.

- `get_phys_dict()` — обязателен: возвращает `{канал: состояние}`
- `RelayInterface` добавляет `change_phys_relay_state()`

При инициализации автоматически обновляют словарь из устройства.

---

### 3. `ControllerInterface`
Объединяет `di_interface` и `relay_interface` (можно один из них). Содержит:

- `update_all()` — обновляет оба интерфейса
- `get_all_states()` — возвращает:
```python
{
  "di": {...},
  "relays": {...}
}
```

---

### 4. `ControllerOperator`
Обёртка над контроллером:
- Обеспечивает **mutex-безопасность**
- Поддерживает **автоматическое обновление** в фоне
- Упрощает вызовы:
  - `change_relay_state()`
  - `get_points()`
  - `get_model()`

---

## 🧱 Паттерны проектирования

### ✅ Adapter
Каждый контроллер адаптирует свой протокол под единый API (`get_phys_dict`, `set_relay` и т.д.)

### ✅ Facade
`ControllerInterface` и `ControllerOperator` предоставляют простой фасад над сложной логикой

---

## 💬 Почему `interface`, а не `di_interface` напрямую?

Ранее `ControllerOperator` хранил `.di_interface`, `.relay_interface` отдельно. Сейчас они объединены под `.interface`, чтобы:
- Упростить работу с контроллером как с единой сущностью
- Скрыть детали реализации
- Упростить расширение и переиспользование

---

## 🧪 Пример жизненного цикла:

```python
controller = MoxaE1214("192.168.60.103")
operator = ControllerOperator(controller)

operator.change_relay_state(0, 1)
print(operator.get_points())
```

---

## 📌 Итого
- Все контроллеры реализуются через одни и те же абстракции
- Расширяемость и поддерживаемость — основа дизайна
- Вся логика разложена по слоям: физика, состояние, интерфейс, обёртка

